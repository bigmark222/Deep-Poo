# Ingestion / ETL

How raw captures are collected, pruned, validated, and transformed into a versioned tensor warehouse. This section links to the capture/prune workflow, schema expectations, and the ETL commands (with env toggles) plus a debug checklist.
